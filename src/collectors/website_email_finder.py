"""
TODO — Extraction d'emails depuis les sites web d'entreprises.

Basé sur la logique de l'ancien find-missing-emails.py (voir legacy/).
"""

from src.utils.logging import logger


def find_emails_from_website(url: str) -> list[str]:
    """TODO — Extraire les emails depuis un site web."""
    logger.info(f"Email finder : pas encore implémenté (TODO). URL: {url}")
    return []
